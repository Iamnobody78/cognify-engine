# Prometheus 抓取配置 — 阶段 C1
global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: "governance-gateway"
    metrics_path: /metrics
    static_configs:
      - targets: ["gateway:9000"]
        labels:
          service: governance-gateway
          layer: governance
