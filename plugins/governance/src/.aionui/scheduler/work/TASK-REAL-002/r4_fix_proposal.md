# R4 约束固化提案（待批准）

> 来源：TASK-REAL-002 真实治理批次 2 —— Builder/Tester 双双 token 截断（0 writes）
> 日期：2026-08-03 · 状态：**PROPOSED**（审查者判定 🔴 高优先，经批准后合并）

---

## 背景：R4 的发现轨迹

| 批次 | 现象 | 处理 |
|:--|:--|:--|
| REAL-001 | Builder v1 READ 截断 0 writes | 固化为 **R1 补丁语义**（指令带完整 diff/锚点） |
| REAL-002 | Builder + Tester **双双**截断 0 writes（补丁语义已生效，但**执行容量**仍不足） | 触发 **R3 协调者兜底**；暴露 **R4 执行容量约束** |

**R4 本质**：R1 解决了"探索浪费"，但真实任务的**编辑规模**（9 锚点 + 2 测试文件重写）仍超单子代理单轮 token 预算。R1 与 R4 是互补维度：R1 管"怎么读"（不探索），R4 管"干多少"（拆规模）。

## 增量 1：协议 §2.7 表格追加 R4 行

```markdown
| R4 | **任务规模超单子代理单轮预算**（Builder/Tester 双双 token 截断 0 writes） | **规模拆分**：>6 锚点或 >1 大文件重写 → 拆分为多次 Spawn（每次 ≤6 锚点）或 Coordinator 直接兜底执行 + 标注"R4 兜底"；禁止让单子代理硬扛大任务 | REAL-002: Builder+Tester 双截断 → R3 兜底恢复；REAL-001: 单 Builder 截断 |
```

**恢复流程更新**（§2.7 恢复流程第 1 行追加判定）：
```markdown
- 子代理截断 → Coordinator 检查落盘产物（file_info）：
  - 产物缺失 + 任务规模 >6 锚点/1 大文件 → **R4 拆分**：拆成多次 Spawn（每次 ≤6 锚点）
    或 Coordinator 兜底执行（应用子代理已确认的锚点设计 + 标注）
  - 产物缺失 + 任务规模小 → 补丁语义重建（注入完整 diff + 锚点，禁止重新探索）
```

## 增量 2：注册表路由仲裁规则追加第 12 条

```markdown
12. 任务规模 >6 锚点或 >1 大文件重写 → **拆分 Spawn**（每次 ≤6 锚点）或 Coordinator 兜底；
    禁止单子代理硬扛大任务（TASK-REAL-002: Builder+Tester 双截断 0 writes 教训）
```

## 增量 3：AUDIT-0016 追加（随本提案合并时执行）

记录：R4 固化 + DEBT-0009/0010 注册（审查者隐含依赖入库）+ 锚点 commit `0e18760`。

## 待批准项

| 增量 | 文件 | 位置 | 类型 |
|:---:|------|------|------|
| 1 | teams_collaboration.md | §2.7 表格追加 R4 + 恢复流程更新 | 补充 |
| 2 | agent_registry.yaml | 路由仲裁规则追加第 12 条 | 新增规则 |
| 3 | audit_log.md | AUDIT-0016 追加 | 记录 |

**批准后执行**：合并三处 → 提交 → 输出状态 → 下一批债务（DEBT-0002/0003/0004 或 B3）待裁决。
