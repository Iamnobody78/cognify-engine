__pycache__/
*.pyc
*.egg-info/
.pytest_cache/
*.db

# virtual environments — never commit
.venv*/
venv/
env/

# certification layer keys (P8) — private key never committed
.keys/

# P2 gpt-researcher runtime config — contains DEEPSEEK_API_KEY, never commit
.env
.env.*
!.env.example

# Meta-Harness runtime artifacts (MH-1/MH-2) — filesystem truth layer
traces/
candidates/

# 生成物: 向量索引 (记忆在仓库外, 但防误拷贝入仓)
memory_index.pkl

# 研究产出落盘 (research_output.md 协议 — 运行时产物)
research_outputs/

# judge 服务运行时日志 (阶段 A 生产化)
judge/*.log
gateway_*.log
