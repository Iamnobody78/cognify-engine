fastapi>=0.115
uvicorn>=0.30
sqlalchemy>=2.0
pydantic>=2.0
pyyaml>=6.0
httpx>=0.27
pytest>=8.0
pytest-cov>=5.0
# 生产级数据库（GAP-2.1）: PostgreSQL 驱动，GOV_DASH_DB_URL 一键切换；SQLite 默认零配置
psycopg[binary]>=3.1
# 可观测性（GAP-1.1）: Prometheus /metrics 端点（governance_* 命名空间）
prometheus-client>=0.20
# RBAC（ARCH-ROUND 2 / GAP-3.1）: JWT 认证 + 密码哈希
pyjwt>=2.8
bcrypt>=4.0
