# Prometheus 抓取配置（bottlesumo-pi 可观测性栈, GAP-1.1）
global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: "bottlesumo-dashboard"
    metrics_path: /metrics
    static_configs:
      - targets: ["backend:8000"]
        labels:
          namespace: governance
