# bottlesumo-pi 环境变量样例（复制为 .env 并按环境修改；.env 不入库）
#
# 12-factor: 所有配置通过环境变量注入，禁止硬编码 secrets。

# ===== 数据库（T0.2 / GAP-2.1）=====
# 方式 A（生产，推荐）: 标准 SQLAlchemy URL —— 一键切换 PostgreSQL
# GOV_DASH_DB_URL=postgresql+psycopg://bottlesumo:bottlesumo@localhost:5432/bottlesumo
# 方式 B（开发/测试，默认）: SQLite 文件路径（保留旧变量名向后兼容）
# GOV_DASH_DB=./db/governance.db

# ===== 服务 =====
# HOST=0.0.0.0
# PORT=8000

# ===== 治理引擎（同进程集成）=====
# GOV_AGENTS_V2_PATH=/path/to/agent-governance-v2

# ===== 可观测性（T0.3 / GAP-1.1）=====
# GOV_METRICS_ENABLED=1        # 暴露 /metrics 端点
# GOV_LOG_FORMAT=json          # 结构化日志: json | plain
